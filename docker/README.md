# Shared Docker Configuration

This directory contains shared configurations, certificate authorities, or compose overrides.

- Place any persistent volumes or shared configs here.
- Dockerfiles are kept in their respective service directories (`/frontend` and `/backend`) to maintain project modularity.
